<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hawk.testzkms">

    <uses-permission android:name="com.htc.wallet.permission.ACCESS_ZION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.hawk.testzkms.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.hawk.testzkms.permission.TargetActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|fontScale"
            android:exported="false" >
        </activity>

        <activity android:name="com.hawk.testzkms.permission.RequestPermissionsActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|fontScale"
            android:exported="false" >
        </activity>

    </application>

</manifest>